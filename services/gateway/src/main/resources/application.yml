spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-url: "http://localhost:9098/realms/micro-service"
  config:
    import: optional:configserver:http://localhost:8888
  application:
    name: gateway-service